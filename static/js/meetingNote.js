// static/js/meetingNote.js
// Meeting-note capture panel — Task 8 stub.
// window.openMeetingNote is set here so people.js can call it via optional
// chaining without errors. The real implementation ships in Task 8.
window.openMeetingNote = window.openMeetingNote || function(opts) {
  console.info('meetingNote: stub — Task 8 not yet implemented', opts);
};
